import {
  contentfulMenu,
  normalizedMenu
} from '~domains/contentful/__mocks__/menu'

export const contentfulHeaderNavigation = {
  items: [contentfulMenu]
}

export const contentfulHeaderNavigationNormalized = normalizedMenu
