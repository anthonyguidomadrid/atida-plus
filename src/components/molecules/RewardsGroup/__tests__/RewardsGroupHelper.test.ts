import { handleClaimReward } from '..'

// TODO: Update with the real methods once they are ready
describe(handleClaimReward, () => {
  it('returns a method to handle the onClick - basic', () => {
    expect(handleClaimReward('basic')).toEqual(expect.any(Function))
  })
  it('returns a method to handle the onClick - advanced', () => {
    expect(handleClaimReward('advanced')).toEqual(expect.any(Function))
  })
})
